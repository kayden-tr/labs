groups:
- name: Blackbox Exporter Rules
  rules:
  - alert: TCPPortDown
    expr: probe_success == 0
    for: 10s
    labels:
      severity: "critical"
    annotations:
      summary: "Endpoint {{ $labels.instance }} is down"
      descriptsion: "{{ $labels.name }} on {{ $labels.instance }} is down"
